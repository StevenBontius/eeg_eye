[project]
name = "eeg-eye"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "StevenBontius", email = "steven@bonti.us" }
]
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pathlib>=1.0.1",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
    "ruff>=0.15.7",
]
